<div class="row clearfix">
    <div class="col-md-10 column">
        <span class="label label-primary" style="font-size: 25px">Users List</span>
        <br><br>
<?php
if($this->users)
{   
    echo "<table class='table table-striped table-hover'>
            <tr>
                <th>Name</th>
                <th>Gender</th>
                <th>Email</th>            
                <th>Telephone</th>
                <th>Mobile</th>
                <th>Type</th>
                <th>Date</th>
                <th>Action</th>
            </tr>";
    $user = $this->users;
    foreach ($this->paginator as $user)
    {
        echo "<tr><td>".$user["name"]."</td>";
        echo "<td>";
        if ($user["sex"] == 'F') {
            echo "Female";
        } elseif ($user["sex"] == 'M') {
            echo "Male";
        }
        echo "</td>";
        echo "<td>".$user["email"]."</td>";
        echo "<td>".$user["telephone"]."</td>";
        echo "<td>".$user["mobile"]."</td>";
        echo "<td>".$user["role"]."</td>";
        echo "<td>".$user["join_date"]."</td>";
        echo "<td><a href='".$this->baseUrl()."/user/delete/userId/".$user['id']."' class='btn btn-default'>Delete</a>";
        echo "<a href='".$this->baseUrl()."/user/edit/userId/".$user['id']."' class='btn btn-default'>Edit</a></td>";
        echo "</tr>";
    }
    echo "</table>";
    echo $this->paginationControl($this->paginator, 'Sliding', 'pagination.phtml');
}
    

?>
    <a href="javascript:history.back();">Back</a>
    </div>
    </div>